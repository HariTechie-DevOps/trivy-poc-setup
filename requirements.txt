# Still vulnerable versions but compatible with python:3.9
# Trivy will still find CVEs in these
Flask==1.0.0
Werkzeug==0.15.0
Jinja2==2.10.1
requests==2.20.0
PyYAML==5.1
urllib3==1.24.0
cryptography==2.5
